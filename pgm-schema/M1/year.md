# year

`year` defines the property key for a calendar year on a relationship. [:Property_Key {identifier: "year"}]()

Concrete values use YAML scalar semantics. Their datatypes are intentionally not modeled as graph nodes.
