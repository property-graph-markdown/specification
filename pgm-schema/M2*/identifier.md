# identifier

`identifier` is the Property Key for the stable identifier of a schema or metaschema definition. This definition uses its own Property Key and thereby also closes the Property typing chain within M2*. [:Property_Key {identifier: "identifier"}]()

The concrete value is a YAML string. No dedicated datatype node is required.
