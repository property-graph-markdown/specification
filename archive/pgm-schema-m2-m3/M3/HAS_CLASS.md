# RANGE

`RANGE` assigns its permitted Target classifier to an Association definition. This definition uses `RANGE` itself and therefore remains closed within M3. [:Association {"symbol":"RANGE"}]()

The Domain is an Association definition.

The Range is a Class. [:HAS_CLASS](M3/Class.md)
