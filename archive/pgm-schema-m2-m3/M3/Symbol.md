# Symbol

`symbol` is the attribute that is unique within a model level and binds a lexical PGM identifier to its definition node. [:Symbol]()

The value of `symbol` is text. [:VALUE_TYPE](String.md)
